server:
  servlet:
    context-path: /user-service

spring:
  config:
    import: ""
  application:
    name: USER-SERVICE

eureka:
  client:
    enabled: false

management:
  health:
    circuitbreakers:
      enabled: false
